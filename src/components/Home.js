import React from "react";
import { name, city } from "../data/data.js";

function Home() {
  // update the JSX being returned!
  return <div>Home</div>
  return <div id="Home">
    <h1 style={{color:'firebrick'}}>{YourName} is a Web Developer from {YourCity}</h1>
  </div>;
}

export default Home;
